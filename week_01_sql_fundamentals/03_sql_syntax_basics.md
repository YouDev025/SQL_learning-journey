# SQL Syntax Basics

SQL is written using standard keywords and clause order. The basic structure is:
```sql
SELECT columns
FROM table
WHERE condition;
```

Example:
```sql
SELECT product_name, price
FROM products
WHERE price > 20;
```
