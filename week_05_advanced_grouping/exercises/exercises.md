# Week 5 Exercises

1. Use GROUP BY and HAVING to find categories with more than 10 items.
2. Write a query with `COALESCE` inside a SUM aggregate.
3. Explain when `HAVING` is needed instead of `WHERE`.
4. Create a query using `GROUPING SETS` for two levels of grouping.
5. Count rows for each `status` and return only groups with totals above 3.

---
Concepts (roadmap summary):
- Filter aggregated results with `HAVING`
- Handle `NULL` values in grouping
- Learn grouping concepts for summary queries
