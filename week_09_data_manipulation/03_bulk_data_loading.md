# Bulk Data Loading

Bulk operations insert many rows at once using multiple VALUES or import tools.

Example:
```sql
INSERT INTO products (name, price)
VALUES
  ('Pen', 1.25),
  ('Notebook', 3.50),
  ('Marker', 2.00);
```
