# Week 10 Exercises

1. Use a window function to calculate a running total.
2. Write a CTE for a query that selects recent sales.
3. Use `UNION` to combine two simple select statements.
4. Explain the difference between `UNION` and `UNION ALL`.
5. Create a query with `INTERSECT` or `EXCEPT` in a short example.

---
Concepts (roadmap summary):
- Explore window functions
- Learn common table expressions (CTEs)
- Use `UNION`, `INTERSECT`, and `EXCEPT` for combined queries
