# Week 4 Exercises

1. Count the number of rows in the `users` table.
2. Calculate the sum of `quantity` in `order_items`.
3. Find the average `rating` in `reviews`.
4. Get the minimum and maximum `salary` from `employees`.
5. Use GROUP BY to count customers by `country`.

---
Concepts (roadmap summary):
- Use `COUNT`, `SUM`, `AVG`, `MIN`, and `MAX`
- Aggregate data with `GROUP BY`
- Summarize result sets effectively
